# 待提交修改代码评审（2026-08-02）

## 背景
本次待提交修改（`git diff` 共 22 个文件、约 +950/−507）对应的是 `CODE_REVIEW.md` 中记录的 11 项问题修复（时区差一天、绝对路径迁移、备份回滚不完整、智能继承覆盖 createdAt、CSV BOM、同 APP 多版本被去重、迁移自检过弱、queryTodos 负责人筛选冲突、临时待办被清空、空触发事件等）+ 额外加固（链接 scheme 白名单、DataCache 定时清理 + destroy）。原"修复复核"判定修复正确且 151 单测通过。

本文档聚焦：**这些修复是否引入了新的问题 / 回归**。

---

## 🔴 必须处理（回归 / 兼容）

### P1. 预发布高亮对已安装用户失效（兼容回归）
- **位置**：`src/types.ts` 的 `PRE_RELEASE_TRIGGERS` 与 `DEFAULT_SETTINGS.preReleaseRound`
- **现象**：默认值由 `'B3'` 改为 `'B3集成测试'`，但已安装用户 vault 中已保存的 `settings.preReleaseRound` 仍是旧值 `'B3'`（Obsidian 设置合并时旧值优先保留）。`isProjectInPreRelease` 中 `PRE_RELEASE_TRIGGERS['B3']` 为 `undefined` → 直接 `return false`。
- **影响**：存量用户升级插件后，**所有项目的预发布提示彻底不显示**，必须手动重开设置切换一次才恢复。
- **建议**：在 `PRE_RELEASE_TRIGGERS` 增加向后兼容 key（`'B1'/'B2'/'B3'/'B4'` → 对应旧触发阶段）；或在设置加载/迁移时把 `'B3'` 等旧值映射为 `'B3集成测试'`。
- **测试缺口**：`tests/types.test.ts` 中所有用例都用新值（`'B3集成测试'` 等），未覆盖旧值 `'B3'` 的回归，所以未被发现。

### P2. 待办增删改后上层项目视图不再刷新（交互回归）
- **位置**：`src/view/TodoTabView.ts`（多处 `this.render(); this.onRefresh?.();` → 仅 `await this.render()`）
- **现象**：移除了 `onRefresh` 调用；而全局事件 `app-version-manager:refresh` **只由 ReminderService 触发**（main.ts 的 `notifyViewsToRefresh` 是提醒回调），待办的 create/update/delete/状态切换并不会触发它。
- **影响**：`AppVersionManagerView` 的项目卡片（待办计数、逾期/预发布标记等）在待办变更后**不同步刷新**，直到手动刷新或恰好触发提醒。
- **建议**：在 `TodoService` 的 create/update/delete 成功后调用 `this.plugin.notifyViewsToRefresh()`（视图已订阅该事件）；或保留 `TodoTabView` 的 `onRefresh` 调用。

---

## 🟡 建议处理（稳健性 / 跨平台）

### P3. Windows + 绝对路径存储下取父目录失败
- **位置**：`TodoService.ensureFileFolder`、`MigrationService.writeNewFile` 使用 `path.substring(0, path.lastIndexOf('/'))`
- **现象**：绝对路径经 `join()` 在 Windows 上为 `\` 分隔，`lastIndexOf('/')` 返回 -1 → `dir` 变成整个路径（含文件名）→ `mkdirSync` 抛错。
- **影响**：仅 Windows 且启用绝对路径数据目录时触发。建议改用 Node `path.dirname(path)`。

### P4. `DataCache.destroy()` 从未被调用
- **现象**：定义了 `destroy()` 清理定时器，但全仓无任何调用点。Obsidian 每次 reload 会累积一个活动 `setInterval`（虽 `unref()` 不阻塞退出，但定时器与闭包引用不释放）。
- **建议**：在 `DataService` / 插件 `onunload` 中调用 `cache.destroy()`。

### P5. `deleteTodoFile` 类型签名与路径逻辑不一致
- **位置**：`TodoService.deleteTodoFile(todo: Pick<Todo,'id'|'content'>)`；内部 `getTodoFilePath` 依赖 `responsiblePerson` 决定子目录。
- **现象**：当前调用方都传完整 `Todo`，运行时安全；但类型允许只传 `{id, content}`，若未来误用会导致负责人子目录下的文件删不掉（产生孤儿文件）。
- **建议**：签名改为 `Todo` 或 `Pick<Todo,'id'|'content'|'responsiblePerson'>`。

---

## 🔵 需确认的行为变更

- **B6. 待办列表日期筛选语义变化**：`TodoTabView` 日期范围由"创建日期（`createdDateFrom/To`）"改为"更新日期（`updatedDateFrom/To`）"，与智能继承改 `updatedAt` 逻辑自洽（被继承的待办会出现在近期），但属语义变更，请确认是否符合 UX 预期。
- **B7. 智能继承受全局负责人筛选影响**：`TodoInheritanceService.runInheritance()` 经 `getAllTodos()` 取待办，而该方法会按 `currentResponsiblePerson` 过滤。若设置了负责人，继承只处理该负责人待办、其他人待办不更新。继承应为全局行为，建议绕过该筛选（类似新增的 `getAllTodosBypassFilter`）。

---

## ✅ 测试覆盖评价
- **覆盖良好**：`isProjectInPreRelease`（日期语义、trigger 为空但后续阶段 past 仍命中）、`getCurrentBRound`（返回完整 label）、`ROUND_COLORS`（全 key）、`DataService`（解析器改用 `readContent`）、`ImportExportService`（BOM / 同 APP 多版本去重）、`BackupService`（todos/categories 回滚）均有用例。
- **缺口**：无旧值 `'B3'` 兼容回归用例（对应 P1）；无"待办变更后视图刷新"相关验证（对应 P2）。

---

## 结论
修复整体方向正确、质量较高。建议**合并前先处理 P1（兼容回归，影响存量用户）和 P2（交互回归，影响日常使用）**；P3–P5 与 B6/B7 可排期。合并前建议跑一次 `npm run build && npm test` 做最终确认。
